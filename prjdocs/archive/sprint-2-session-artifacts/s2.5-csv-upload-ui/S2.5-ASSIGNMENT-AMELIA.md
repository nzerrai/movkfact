---
title: "📌 S2.5 ASSIGNMENT - Amelia (Backend)"
titleFR: "📌 S2.5 ASSIGNATION - Amelia (Backend)"
date: "2026-02-28"
time: "12:50 CET"
recipient: "Amelia (Developer)"
sprint: "Sprint 2"
story: "S2.5 (+ S2.4 work)"
role: "Backend Verification & Code Review"
effort: "0.5-1 day S2.5 support + 1 day S2.4"
status: "ASSIGNMENT"
---

# 📌 S2.5 ASSIGNMENT - Backend Support & Code Review

**From:** Bob (Scrum Master)  
**To:** Amelia (Developer)  
**Date:** 28 février 2026 @ 12:50 CET  
**Sprint 2 Start:** 17 mars 2026

---

## 🎯 YOUR MISSION

Sally (UX Designer) will build the CSV Upload UI (S2.5). Your role is to:

1. **Verify** S2.2 Type Detection API is production-ready
2. **Test** API performance with different file sizes
3. **Support** Sally during frontend integration
4. **Code Review** her component before merge
5. **Execute** S2.4 JSON Export in parallel

---

## ✅ TASK 1: API VERIFICATION (Day 1 Morning - 17 mars)

### Endpoint Check

```
Endpoint: POST /api/domains/{domainId}/detect-types
Content-Type: multipart/form-data
```

**Verification Checklist:**

- [ ] Endpoint exists and responds to requests
- [ ] Response format correct:
  ```json
  {
    "data": {
      "columns": [
        {
          "name": "customer_id",
          "type": "PERSONAL_ID",
          "confidence": 0.95,
          "detector": "PersonalDataDetector"
        }
      ],
      "processedRows": 1000,
      "successRate": 0.98
    },
    "message": "Type detection completed"
  }
  ```
- [ ] HTTP Status codes handled (200, 400, 500)
- [ ] CORS headers present (Allow requests from port 3000)
- [ ] File upload validation works (reject invalid files)

---

## ⏱️ TASK 2: PERFORMANCE BENCHMARKING (Day 1 Morning)

### Performance Requirements

```
Target Performance:
  - Small files (100 rows):    < 500ms
  - Medium files (1000 rows):  < 2 seconds
  - Large files (10K rows):    < 5 seconds (AC1 requirement)
  
Success Rate: > 95% of columns correctly detected
```

### Run Benchmark Tests

**Test 1: Small CSV (100 rows)**
```bash
curl -X POST http://localhost:8080/api/domains/1/detect-types \
  -F "file=@test-100rows.csv" \
  --verbose 2>&1 | grep "< HTTP\|Total time"
# Expected: < 500ms ✅
```

**Test 2: Medium CSV (1000 rows)**
```bash
curl -X POST http://localhost:8080/api/domains/1/detect-types \
  -F "file=@test-1000rows.csv" \
  --verbose 2>&1 | grep "< HTTP\|Total time"
# Expected: < 2 seconds ✅
```

**Test 3: Large CSV (10K rows)**
```bash
curl -X POST http://localhost:8080/api/domains/1/detect-types \
  -F "file=@test-10000rows.csv" \
  --verbose 2>&1 | grep "< HTTP\|Total time"
# Expected: < 5 seconds ✅
```

### Benchmark Report

Document results in: `/tmp/s2.5_api_performance.txt`

```
S2.5 API Performance Report
============================

Test 1 (100 rows):       XXXms  [✅ / ❌]
Test 2 (1000 rows):      XXXms  [✅ / ❌]
Test 3 (10000 rows):     XXXms  [✅ / ❌]

CORS Headers:            [✅ / ❌]
Response Format:         [✅ / ❌]
Error Handling:          [✅ / ❌]

Status: READY FOR INTEGRATION / NEEDS FIXES
Issues: (list any problems)
```

---

## 🔗 TASK 3: INTEGRATION SUPPORT (Day 1 Afternoon - Ongoing)

**Sally Will Ask:**

1. **"Can I call endpoint with file from React file input?"**
   ```javascript
   const formData = new FormData();
   formData.append('file', file);
   const response = await fetch(`/api/domains/${domainId}/detect-types`, {
     method: 'POST',
     body: formData,
     // NO Content-Type header! Browser sets it automatically
   });
   ```
   Your Answer: ✅ "Correct. Don't manually set Content-Type."

2. **"How do I handle CORS errors?"**
   Your Answer: "If you see CORS errors in console, let me know. Backend should be configured with @CrossOrigin. Check Access-Control-Allow-Origin header."

3. **"What if file upload fails?"**
   Your Answer: "Check response.status:
   - 400 = Invalid file format
   - 413 = File too large
   - 500 = Server error
   All return proper error messages."

---

## 👀 TASK 4: CODE REVIEW (Day 2-3 Afternoon)

When Sally submits her PR, review for:

- [ ] Correct API endpoint URL
- [ ] Correct HTTP method (POST)
- [ ] FormData correctly constructed
- [ ] Response parsing matches API structure
- [ ] Error handling covers main cases
- [ ] CORS issues won't occur
- [ ] No hardcoded URLs (use env vars)
- [ ] API calls have timeouts
- [ ] Loading states prevent double-submit
- [ ] UI remains responsive during API call

---

## 🔄 TASK 5: PARALLEL WORK - S2.4 JSON EXPORT

### Timeline Coordination

```
Day 1 (17 mars):
  Morning  (2h): Verify S2.5 API + benchmarks
  Afternoon(3h): Start S2.4 JSON export backend
             (1h): Support Sally Q&A
    
Day 2-3 (18-20 mars):
  Morning  (2h): Continue S2.4 work
  Afternoon(2h): Code review Sally's S2.5
             (1h): S2.4 final touches
    
EOD 20 mars: Both S2.4 ✅ + S2.5 ✅ DONE
```

### S2.4 Implementation (After S2.5 API Verified)

**Scope:** Create JSON export endpoint

```java
// S2.4 New Endpoint
@PostMapping("/{domainId}/export/json")
public ResponseEntity<ApiResponse<String>> exportDomainJson(
  @PathVariable Integer domainId
) {
  String json = exportService.exportDomainAsJson(domainId);
  return ResponseEntity.ok(
    new ApiResponse<>(json, "Domain exported as JSON")
  );
}
```

**Don't Wait for Sally:** Once S2.5 API verified (morning), start S2.4 immediately

---

## 📋 CHECKLIST - DAY 1 (17 mars)

**Morning (08:00-12:00):**
- [ ] Verify S2.5 API endpoint exists
- [ ] Test CORS headers
- [ ] Run performance benchmarks (3 scenarios)
- [ ] Document results in `/tmp/s2.5_api_performance.txt`
- [ ] Message Sally: "API verified ✅ Ready for integration"

**Afternoon (13:00-17:00):**
- [ ] Start S2.4 JSON export implementation
- [ ] Answer Sally's API questions in real-time
- [ ] Test S2.4 endpoints locally

---

## 📋 CHECKLIST - DAYS 2-3 (18-20 mars)

**Morning:**
- [ ] Continue S2.4 implementation
- [ ] Write S2.4 unit tests
- [ ] Answer any Sally questions

**Afternoon:**
- [ ] Code review Sally's CSVUploadPanel component
- [ ] Complete S2.4 testing
- [ ] Merge S2.5 (if approved)
- [ ] Merge S2.4 (if complete)

---

## 🚀 READY TO START

```
Status: ✅ ASSIGNMENT READY

Start Date: 17 mars 2026 (Sprint 2 kickoff 09:00)

Day 1 Morning Action:  API Verification + Performance Tests
Day 1 Afternoon Action: Start S2.4 + Support Sally
Days 2-3 Action:       Code Review S2.5 + Finish S2.4

Expected Outcome: S2.5 ✅ + S2.4 ✅ by EOD 20 mars
```

---

**Assignment From:** Bob (Scrum Master)  
**Date:** 28 février 2026 @ 12:50 CET

